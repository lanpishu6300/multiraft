[package]
name = "multiraft-plugin-archive"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Snapshot catalog archive ops plugin for multiraft"

[dependencies]
multiraft-core = { path = "../multiraft-core" }
multiraft-store = { path = "../multiraft-store" }
serde = { workspace = true }

[dev-dependencies]
tempfile = "3"
